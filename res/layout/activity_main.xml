<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools" android:id="@+id/myLayout"
                android:orientation="vertical"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                tools:context=".main.MainActivity">

    <Spinner
            android:id="@+id/countries"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_toLeftOf="@+id/cities"
            android:prompt = "@+id/prompt_country"
            />

    <Spinner
            android:id="@+id/cities"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:prompt = "@+id/prompt_city"
            android:layout_alignParentRight="true"/>

    <Spinner
            android:id="@+id/categories"
            android:layout_below="@id/countries"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:prompt = "@+id/prompt_category"
            android:layout_alignParentRight="true"/>

    <Button
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:layout_below="@id/categories"
            android:id="@+id/map"
            android:text="@string/map_button_text">
    </Button>


    <ListView android:id="@+id/offers_view"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:fastScrollEnabled="true"
              android:layout_below="@+id/map"
            >
    </ListView>


</RelativeLayout>